# CSS Stylesheets

This directory contains CSS style files for the application:

- `application.css` - Main application styles
- `light-theme.css` - Light theme colors and styling
- `forms.css` - Form input styling
- `tables.css` - Table view styling
- `buttons.css` - Button styling

CSS files customize the look and feel of the JavaFX application.

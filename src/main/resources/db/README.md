# Database Resources

This directory contains database-related resources:

- `schema.sql` - SQLite database schema (table definitions)
- `initial_data.sql` - Initial/seed data (optional)

The schema.sql file will be used to initialize the database on first run.
